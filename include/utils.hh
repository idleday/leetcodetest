#ifndef UTILS_HH
#define UTILS_HH

#include <vector>
#include <string>

using namespace std;

template<typename T> void vector_dump(vector<T>& v);
vector<int> string2vector(string s);
#endif // UTILS_HH
